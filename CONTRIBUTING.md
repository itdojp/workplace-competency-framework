# 変更の進め方

対象は[itdojp/workplace-competency-framework](https://github.com/itdojp/workplace-competency-framework)です。公開用の文書・教材はGitHubのブラウザ上で確認・編集し、`main`をマージ先とする短命branchとPRで更新します。PCへの取得やGitのインストールは必須ではありません。承認や保護設定の強制は、この文書だけでは保証されません。

## 前提と変更範囲

実行者は公開内容を確認できる保守担当者、実行環境はGitHubへログインしたブラウザです。通常の利用・変更開始点は`main`です。変更branchとPR番号は作成後の実在する値を記録します。PRを作成するまでは「未作成」とし、マージ済みの初期登録branchを再利用しません。

変更するファイルはリポジトリルートからの相対パスとGitHubリンクで特定します。対象外の教材本文、個人データ、Google設定、Organizationの権限・課金、リポジトリの公開範囲は変更しません。

## ブラウザでの更新

1. **GitHubへ内容を送る前に**[公開方針](docs/publication-policy.md)を確認します。新しいbranchやDraft PRも公開対象です。個人・顧客・認証情報、採用本番の未公開問題を編集欄へ入れません。
2. `main`で対象ファイルを開き、基準コミットと変更目的を記録します。GitHub上で変更用branchを作成し、ファイルの編集画面でそのbranchを選びます。編集権限がなければ作業を止めて保守担当者へ確認し、権限や保護設定を自己変更しません。
3. 本文を編集し、Previewと差分を確認して変更用branchへコミットします。関連ファイルも同じbranchで更新します。`main`への直接コミットはしません。
4. baseを`main`、headを変更用branchとしてPRを作成します。[PRテンプレート](.github/PULL_REQUEST_TEMPLATE.md)に従い、ファイルパス、変更禁止対象、確認結果、未実施事項、停止・復旧方法を記載します。
5. PRのFiles changedで、意図したファイルと内容だけが変わっていること、リンク先が存在することを確認します。レビュー後、権限を持つ担当者がマージを判断します。文書の確認をGemini実機受入や学習効果の検証と呼ばないでください。

期待結果は、説明と参照がGitHub上でつながり、変更理由・差分・確認結果をPRから追跡できることです。

## 文書と教材の規則

参照はリンク付きのリポジトリ相対パスを基本とし、試行・検証の記録にはコミット固定のGitHub URLを使用します。端末内の絶対パス、ダウンロード・展開、過去の会話添付、リポジトリにないブックを必須手順にしません。文書だけを閲覧する利用者に、開発用環境の準備を求めません。

能力ID、重要度、AI増分、架空資料の条件、評価基準を変更する場合は、理由、影響先、再確認方法をPRに示します。教材内容の改訂と参照・配置の変更を区別します。開始文の指示は開発エージェントへの命令ではなく、教材としてレビューします。

Issueの不具合報告には教材ID、版、コミット固定の参照、公開してよい架空の再現入力、期待した動作、実際の動作を記載します。社員の回答・会話・評価画面や内部保存先は貼りません。

ライセンス変更やプログラム追加は会社の承認と適用範囲を確認します。第三者資料の全文をコピーしません。コード・workflowを文書修正に便乗して追加しません。

## エラー時・復旧

コミット前なら編集を取り消します。コミット後・未マージならPRをマージせず、同じ変更branchで訂正します。他の変更が入っていたら差分を再確認し、上書きしません。

マージ後の通常の誤編集は、対象ファイルのHistoryで戻す内容を確認し、復元内容を新しいbranchとPRで提案します。履歴を強制的に書き換えません。誤公開は通常の復元で回収できたとは扱わず、[SECURITY.md](SECURITY.md)と[公開方針](docs/publication-policy.md)に従って非公開経路で報告します。

参考：[GitHub上でファイルを編集する](https://docs.github.com/en/repositories/working-with-files/managing-files/editing-files)、[コミット固定リンク](https://docs.github.com/en/repositories/working-with-files/using-files/getting-permanent-links-to-files)。
